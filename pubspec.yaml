name: siriustrackeragente
description: Aplicativo de suporte para agentes recolhedores de veiculos.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_auth: ^0.15.5+2
  shared_preferences: ^0.5.6+2
  google_sign_in: ^4.1.4
  cloud_firestore: ^0.13.4+1
  google_maps_flutter: ^0.5.24+1
  location: ^2.5.3  #não possui suporte para web
  flutter_money_formatter: ^0.8.3
  image_picker: ^0.6.3+4 #não possui suporte para web
  firebase_storage: ^3.1.3 #não possui suporte para web
  cached_network_image: ^2.0.0 #não possui suporte para web
  intl: ^0.15.8
  flutter_masked_text: ^0.8.0
  image_cropper: 1.0.2
  flutter_credit_card: ^0.1.3
  url_launcher: ^5.4.2
  http: ^0.12.0+1
  json_serializable: ^2.0.2

  cupertino_icons: ^0.1.2

dev_dependencies:
  pedantic: ^1.4.0
  build_runner: ^1.2.7
  flutter_test:
    sdk: flutter

flutter:
  fonts:
    - family: Raleway
      fonts:
        - asset: assets/fonts/Raleway-Regular.ttf
        - asset: assets/fonts/Raleway-Italic.ttf
          style: italic
    - family: WorkSans
      fonts:
        - asset: assets/fonts/WorkSans-Regular.ttf
        - asset: assets/fonts/WorkSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/WorkSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/WorkSans-Bold.ttf
          weight: 700

  assets:
    - assets/images/default.png
    - assets/images/google_logo.png
    - assets/images/mylocation.png
    - assets/images/cnh.png
    - assets/images/img_not_available.jpeg
    - assets/images/card.png
    - assets/images/contact.png
    - assets/images/configuration.png
    - assets/images/information.png
    - assets/images/notes.png
    - assets/images/conversation.png
    - assets/images/nodata.png
    - assets/images/profile.png
    - assets/icon/app.gif

  uses-material-design: true
